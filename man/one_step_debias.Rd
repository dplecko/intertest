% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/one_step_debias.R
\name{one_step_debias}
\alias{one_step_debias}
\title{One-step debiased estimator for potential outcome means}
\usage{
one_step_debias(
  data,
  X,
  Z = character(0),
  W = character(0),
  Y,
  scale = c("difference", "log-risk", "log-odds"),
  nested_mean = c("refit", "wregr"),
  eps_trim = 0,
  cfit = NULL,
  ...
)
}
\arguments{
\item{data}{A numeric data frame (all columns must be numeric or integer; use
\code{inter_test()} for automatic factor encoding).}

\item{X}{Name of the binary exposure variable.}

\item{Z}{Character vector of pre-exposure confounder names. Pass
\code{character(0)} if absent.}

\item{W}{Character vector of mediator names. Pass \code{character(0)} if absent.}

\item{Y}{Name of the outcome variable.}

\item{scale}{One of \code{"difference"}, \code{"log-risk"}, or \code{"log-odds"}.}

\item{nested_mean}{How to estimate the nested conditional mean
E[E[Y(x') | X=x, Z, W] | X=x, Z]. \code{"refit"} (default) regresses
cross-fitted outcome predictions on (X, Z); \code{"wregr"} uses a weighted
regression on Z only (not available for \code{"log-risk"}).}

\item{eps_trim}{Propensity score trimming threshold. Observations with
P(X=x | Z) or P(X=x | Z, W) below this value are excluded. Default \code{0}
(no trimming).}

\item{cfit}{Optional pre-computed cross-fit object (output of the internal
\code{cross_fit()} function). When supplied, the cross-fitting step is skipped.
Useful when \code{inter_test()} shares one cross-fit across multiple scales.}

\item{...}{Additional arguments passed to \code{xgb.cv} / \code{xgb.train} (e.g.
\code{nthread}).}
}
\value{
A data frame with columns \code{measure}, \code{value}, \code{sd}, and \code{scale}, one
row per effect. The data frame carries a \code{"pw"} attribute with the fitted
propensity weights.
}
\description{
Workhorse estimator for a single scale. Cross-fits nuisance functions via
gradient boosting and returns a data frame of effect estimates with standard
errors.
}
