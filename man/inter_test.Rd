% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inter_test.R
\name{inter_test}
\alias{inter_test}
\title{Test for interaction effects in a causal graph}
\usage{
inter_test(
  data,
  X,
  Z = character(0),
  W = character(0),
  Y,
  scale = "difference",
  nested_mean = c("refit", "wregr"),
  eps_trim = 0,
  preproc = TRUE,
  save_pso = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame. Numeric and integer columns are used as-is.
Logical, character, and factor columns are auto-encoded (binary factors to
0/1; multi-level factors to dummy columns) and a message is emitted
listing the encoded variables. Pass \code{preproc = FALSE} to disable this
behaviour and supply a fully numeric data frame.}

\item{X}{Name of the binary exposure variable (character scalar).}

\item{Z}{Character vector of pre-exposure confounder names. Pass
\code{character(0)} (default) if absent.}

\item{W}{Character vector of mediator names. Pass \code{character(0)} (default)
if absent.}

\item{Y}{Name of the outcome variable (character scalar).}

\item{scale}{Character vector, one or more of \code{"difference"}, \code{"log-risk"},
\code{"log-odds"}. Nuisance estimation is shared across \code{"difference"} and
\code{"log-odds"} but is run separately for \code{"log-risk"}. Default
\code{"difference"}.}

\item{nested_mean}{How to estimate the nested conditional mean. \code{"refit"}
(default) regresses cross-fitted outcome predictions on (X, Z); \code{"wregr"}
uses a weighted regression on Z only (not compatible with \code{"log-risk"}).}

\item{eps_trim}{Propensity score trimming threshold. Default \code{0} (no
trimming).}

\item{preproc}{Logical. If \code{TRUE} (default), non-numeric columns are
auto-encoded and a message is emitted. Set to \code{FALSE} to bypass encoding
entirely.}

\item{save_pso}{Logical. If \code{TRUE}, pseudo-outcomes for each potential
outcome are stored in the returned object (useful for diagnostics). Default
\code{FALSE}.}

\item{...}{Additional arguments forwarded to \code{xgb.cv} / \code{xgb.train} (e.g.
\code{nthread}).}
}
\value{
An object of class \code{"intertest"}, a list with components:
\describe{
\item{\code{results}}{Data frame with columns \code{measure}, \code{value}, \code{sd},
\code{scale}, and \code{p_value}.}
\item{\code{pso}}{List of pseudo-outcomes, one element per scale (only
populated when \code{save_pso = TRUE}).}
\item{\code{call}}{The matched call.}
\item{\code{graph}}{Named list \code{list(X, Z, W, Y)} giving the variable groups
after any encoding.}
\item{\code{n}}{Number of observations used (after trimming).}
}
}
\description{
Main entry point for the \code{intertest} package. Estimates and tests for
pairwise and higher-order interaction effects between the direct effect (DE),
indirect effect (IE), and spurious effect (SE) of a binary exposure X on
outcome Y, mediated through confounders Z and mediators W.
}
\examples{
\dontrun{
set.seed(1)
n <- 500
Z <- rnorm(n); X <- rbinom(n, 1, plogis(Z))
W <- X + Z + rnorm(n); Y <- X * W + rnorm(n)
dat <- data.frame(Z = Z, X = X, W = W, Y = Y)
fit <- inter_test(dat, X = "X", Z = "Z", W = "W", Y = "Y")
summary(fit)
}

}
